export const metadata = {
  title: 'Publish Extension - Loot Protocol',
  description: 'Publish your AI agent extension to the Loot Protocol marketplace',
};

export default function PublishLayout({ children }: { children: React.ReactNode }) {
  return children;
}
